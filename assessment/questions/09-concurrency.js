window.registerTopic('Concurrency & Threading', [
  { id: 'conc-1', difficulty: 'Basic', type: 'boolean',
    title: '<code>Arc&lt;T&gt;</code> is the thread-safe equivalent of <code>Rc&lt;T&gt;</code>.',
    correct: true,
    studyRef: { readme: '#smart-pointers' } },
  { id: 'conc-2', difficulty: 'Basic', type: 'single',
    title: 'Which marker trait indicates a type can be safely transferred across thread boundaries?',
    choices: ['Sync', 'Send', 'Copy', 'Unpin'],
    correct: 'Send',
    studyRef: { cheats: 'https://cheats.rs/#threading' } },
  { id: 'conc-3', difficulty: 'Intermediate', type: 'single',
    title: 'What does the <code>Sync</code> marker trait mean?',
    choices: [
      'Values of this type can be moved across threads',
      'Shared references to this type can safely be accessed concurrently from multiple threads',
      'This type is synchronized with a mutex',
      'This type implements synchronous I/O',
    ],
    correct: 'Shared references to this type can safely be accessed concurrently from multiple threads',
    studyRef: { cheats: 'https://cheats.rs/#threading' } },
  { id: 'conc-4', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>Rc&lt;T&gt;</code> is <code>!Send</code> and <code>!Sync</code> regardless of whether T is Send or Sync.',
    correct: true,
    studyRef: { readme: '#smart-pointers' } },
  { id: 'conc-5', difficulty: 'Intermediate', type: 'multi',
    title: 'When T is <code>!Send</code>, which of the following are valid strategies to use T across threads? (select all that apply)',
    choices: [
      'Recreate T in each thread if creation is cheap',
      'Store T in thread_local! slots',
      'Use unsafe impl Send for a newtype wrapping T (if safety can be guaranteed)',
      'Clone T before spawning the thread',
      'Use mpsc channels to marshal operations on T to its owning thread',
    ],
    correct: [
      'Recreate T in each thread if creation is cheap',
      'Store T in thread_local! slots',
      'Use unsafe impl Send for a newtype wrapping T (if safety can be guaranteed)',
      'Use mpsc channels to marshal operations on T to its owning thread',
    ],
    studyRef: { readme: '#tricky-threading-cases' } },
  { id: 'conc-6', difficulty: 'Intermediate', type: 'single',
    title: 'All raw pointer types (<code>*mut T</code>, <code>*const T</code>) are:',
    choices: [
      'Send and Sync if T: Send',
      '!Send and !Sync unconditionally',
      'Send only if T: Copy',
      'Sync unconditionally',
    ],
    correct: '!Send and !Sync unconditionally',
    studyRef: { readme: '#raw-pointers' } },
  { id: 'conc-7', difficulty: 'Advanced', type: 'single',
    title: 'Why should spinlocks generally be avoided in user-space Rust programs?',
    choices: [
      'Spinlocks are not available in the std library',
      'Spinning wastes CPU cycles and can cause priority inversion with the OS scheduler',
      'Spinlocks cannot be used with Arc',
      'Spinlocks are slower than Mutex in all cases',
    ],
    correct: 'Spinning wastes CPU cycles and can cause priority inversion with the OS scheduler',
    studyRef: { readme: '#spinlocks' } },
  { id: 'conc-8', difficulty: 'Advanced', type: 'code',
    title: 'What does this code do?',
    code: 'use std::sync::{Arc, Mutex};\nlet m = Arc::new(Mutex::new(0u32));\nlet m2 = Arc::clone(&m);\nstd::thread::spawn(move || {\n    *m2.lock().unwrap() += 1;\n}).join().unwrap();\nprintln!("{}", m.lock().unwrap());',
    choices: ['Prints 0', 'Prints 1', 'Deadlocks', 'Compile error'],
    correct: 'Prints 1',
    studyRef: { readme: '#smart-pointers' } },
  { id: 'conc-9', difficulty: 'Expert', type: 'boolean',
    title: 'If the <code>std::sync::Mutex</code> lock-holder thread panics, subsequent calls to <code>lock()</code> will return an <code>Err(PoisonError)</code>.',
    correct: true,
    studyRef: { readme: '#mutex' } },
  { id: 'conc-10', difficulty: 'Expert', type: 'single',
    title: 'What does a <code>Barrier</code> do in std::sync?',
    choices: [
      'Limits the number of concurrent threads',
      'Blocks n-1 threads until the nth thread arrives, then releases all simultaneously',
      'Provides a one-shot channel between threads',
      'Implements compare-and-swap on a shared integer',
    ],
    correct: 'Blocks n-1 threads until the nth thread arrives, then releases all simultaneously',
    studyRef: { readme: '#other-sync-primitives' } },
]);
