window.registerTopic('Performance & Build', [
  { id: 'perf-1', difficulty: 'Basic', type: 'boolean',
    title: 'You should always use the <code>--release</code> flag when building Rust code for production or performance benchmarking.',
    correct: true,
    studyRef: { readme: '#compilation-options' } },
  { id: 'perf-2', difficulty: 'Basic', type: 'single',
    title: 'What does Link-Time Optimization (LTO) do in Rust?',
    choices: [
      'Speeds up incremental compilation',
      'Enables whole-program optimization across crate boundaries, potentially improving performance by ~20%',
      'Reduces the number of dependencies',
      'Enables parallel codegen',
    ],
    correct: 'Enables whole-program optimization across crate boundaries, potentially improving performance by ~20%',
    studyRef: { readme: '#compilation-options' } },
  { id: 'perf-3', difficulty: 'Intermediate', type: 'single',
    title: 'What does <code>RUSTFLAGS="-C target-cpu=native"</code> do?',
    choices: [
      'Enables debug symbols',
      'Generates machine code optimized for the current CPU, using its specific instruction set extensions',
      'Disables all optimizations',
      'Enables cross-compilation',
    ],
    correct: 'Generates machine code optimized for the current CPU, using its specific instruction set extensions',
    studyRef: { readme: '#compilation-options' } },
  { id: 'perf-4', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>Cow&lt;\'a, str&gt;</code> avoids cloning the underlying string data as long as only read access is needed.',
    correct: true,
    studyRef: { readme: '#heap-allocations' } },
  { id: 'perf-5', difficulty: 'Intermediate', type: 'multi',
    title: 'Which of the following strategies reduce heap allocations for <code>Vec</code> and <code>String</code>? (select all that apply)',
    choices: [
      'Use Vec::with_capacity() when size is known',
      'Use smallvec for typically-small collections',
      'Always use Box<Vec<T>>',
      'Use String::from for all string allocations',
      'Use Cow<str> for read-mostly string data',
    ],
    correct: [
      'Use Vec::with_capacity() when size is known',
      'Use smallvec for typically-small collections',
      'Use Cow<str> for read-mostly string data',
    ],
    studyRef: { readme: '#heap-allocations' } },
  { id: 'perf-6', difficulty: 'Intermediate', type: 'single',
    title: 'Why might you choose <code>Box&lt;[T]&gt;</code> or <code>Rc&lt;[T]&gt;</code> over <code>Vec&lt;T&gt;</code> when the slice will never grow?',
    choices: [
      'They support parallel iteration',
      'They have lower memory overhead — no capacity field',
      'They implement Copy',
      'They are always faster to index',
    ],
    correct: 'They have lower memory overhead — no capacity field',
    studyRef: { readme: '#heap-allocations' } },
  { id: 'perf-7', difficulty: 'Advanced', type: 'single',
    title: 'What is the purpose of Profile-Guided Optimization (PGO)?',
    choices: [
      'Profiling during tests to find slow unit tests',
      'Compiling twice: once to gather runtime profiling data, then using that data to guide a second, more targeted compilation',
      'Automatically parallelizing hot loops',
      'Inlining functions based on their size',
    ],
    correct: 'Compiling twice: once to gather runtime profiling data, then using that data to guide a second, more targeted compilation',
    studyRef: { readme: '#pgo' } },
  { id: 'perf-8', difficulty: 'Advanced', type: 'code',
    title: 'Setting <code>panic = \'abort\'</code> in <code>[profile.release]</code> affects binary size. Why does it NOT work for <code>wasm-pack build</code>?',
    code: '[profile.release]\npanic = \'abort\'',
    choices: [
      'wasm-pack overrides all profile settings',
      'wasm targets require a custom panic handler or nightly -Z options; panic=abort is not supported by wasm-pack build',
      'abort is unsupported on all embedded targets',
      'wasm-pack always uses the debug profile',
    ],
    correct: 'wasm targets require a custom panic handler or nightly -Z options; panic=abort is not supported by wasm-pack build',
    studyRef: { readme: '#reduce-binary-size' } },
  { id: 'perf-9', difficulty: 'Expert', type: 'boolean',
    title: 'The default hashing algorithm in <code>std::collections::HashMap</code> (SipHash 1-3) is optimized for speed on short integer keys rather than for DoS resistance.',
    correct: false,
    studyRef: { readme: '#hashing' } },
  { id: 'perf-10', difficulty: 'Expert', type: 'single',
    title: 'What does <code>cargo build --timings</code> produce?',
    choices: [
      'A runtime CPU profiler report',
      'An HTML report showing per-crate compilation times and parallelism',
      'A list of hot functions in the binary',
      'A summary of LTO passes',
    ],
    correct: 'An HTML report showing per-crate compilation times and parallelism',
    studyRef: { readme: '#speed-up-build' } },
]);
