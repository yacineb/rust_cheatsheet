window.registerTopic('Iterators & Collections', [
  { id: 'iter-1', difficulty: 'Basic', type: 'boolean',
    title: '<code>Iterator::map</code> is lazy: it does not evaluate the closure until the iterator is consumed.',
    correct: true,
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-2', difficulty: 'Basic', type: 'single',
    title: 'Which method consumes an iterator and collects all elements into a <code>Vec</code>?',
    choices: ['fold()', 'collect()', 'for_each()', 'zip()'],
    correct: 'collect()',
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-3', difficulty: 'Intermediate', type: 'single',
    title: '<code>IntoIterator</code> is reflexive. What does this mean?',
    choices: [
      'All iterators implement IntoIterator by returning themselves',
      'IntoIterator is the same as Iterator',
      'All types automatically implement IntoIterator',
      'into_iter() always returns a consuming iterator',
    ],
    correct: 'All iterators implement IntoIterator by returning themselves',
    studyRef: { readme: '#rust-conversions' } },
  { id: 'iter-4', difficulty: 'Intermediate', type: 'multi',
    title: 'Which iterator adapter methods are <em>lazy</em> (do not consume the iterator immediately)? (select all that apply)',
    choices: ['map()', 'filter()', 'collect()', 'sum()', 'take()', 'count()'],
    correct: ['map()', 'filter()', 'take()'],
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-5', difficulty: 'Intermediate', type: 'code',
    title: 'What does this print?',
    code: 'let v = vec![1, 2, 3, 4, 5];\nlet s: i32 = v.iter().filter(|&&x| x % 2 == 0).map(|&x| x * 2).sum();\nprintln!("{s}");',
    choices: ['12', '6', '30', '15'],
    correct: '12',
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-6', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>HashMap</code> in Rust\'s standard library preserves insertion order.',
    correct: false,
    studyRef: { cheats: 'https://cheats.rs/#standard-library' } },
  { id: 'iter-7', difficulty: 'Advanced', type: 'single',
    title: 'What is the difference between <code>iter()</code>, <code>iter_mut()</code>, and <code>into_iter()</code> on a <code>Vec&lt;T&gt;</code>?',
    choices: [
      'They are identical; iter() is just an alias',
      'iter() yields &T, iter_mut() yields &mut T, into_iter() yields T (consuming the Vec)',
      'into_iter() returns a double-ended iterator; the others do not',
      'iter_mut() consumes the Vec; iter() does not',
    ],
    correct: 'iter() yields &T, iter_mut() yields &mut T, into_iter() yields T (consuming the Vec)',
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-8', difficulty: 'Advanced', type: 'single',
    title: 'What does <code>Iterator::flat_map</code> do?',
    choices: [
      'Maps each element to an iterator and flattens the result into a single iterator',
      'Maps each element and removes duplicates',
      'Maps each element and applies filter()',
      'Equivalent to map() followed by collect()',
    ],
    correct: 'Maps each element to an iterator and flattens the result into a single iterator',
    studyRef: { cheats: 'https://cheats.rs/#iterators' } },
  { id: 'iter-9', difficulty: 'Expert', type: 'single',
    title: 'When pre-allocating a <code>Vec</code> with <code>Vec::with_capacity(n)</code>, what is the difference between <code>len()</code> and <code>capacity()</code> immediately after construction?',
    choices: [
      'len() == n, capacity() == n',
      'len() == 0, capacity() >= n',
      'len() == n, capacity() > n',
      'Both are 0',
    ],
    correct: 'len() == 0, capacity() >= n',
    studyRef: { readme: '#heap-allocations' } },
  { id: 'iter-10', difficulty: 'Expert', type: 'boolean',
    title: '<code>BTreeMap</code> iterates in sorted key order, making it suitable as a replacement for <code>HashMap</code> when ordering matters.',
    correct: true,
    studyRef: { cheats: 'https://cheats.rs/#standard-library' } },
]);
