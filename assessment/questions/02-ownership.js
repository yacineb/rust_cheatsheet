window.registerTopic('Ownership, Borrowing & Moves', [
  { id: 'own-1', difficulty: 'Basic', type: 'boolean',
    title: 'A shared reference <code>&T</code> is <code>Copy</code>.',
    correct: true,
    studyRef: { readme: '#borrowing' } },
  { id: 'own-2', difficulty: 'Basic', type: 'single',
    title: 'How many active <code>&mut T</code> to the same value may exist in one scope?',
    choices: ['Zero', 'Exactly one', 'Unlimited', 'One per thread'],
    correct: 'Exactly one',
    studyRef: { readme: '#borrowing' } },
  { id: 'own-3', difficulty: 'Intermediate', type: 'code',
    title: 'What happens when this is compiled?',
    code: 'let s = String::from("hi");\nlet r = &s;\ndrop(s);\nprintln!("{r}");',
    choices: ['Prints "hi"', 'Fails to compile (use after move/drop)', 'Panics at runtime', 'Prints garbage'],
    correct: 'Fails to compile (use after move/drop)',
    studyRef: { cheats: 'https://cheats.rs/#ownership' } },
  { id: 'own-4', difficulty: 'Intermediate', type: 'single',
    title: 'When you pass a <code>String</code> to a function without any reference, what happens to the original binding?',
    choices: [
      'The original binding remains valid',
      'The value is cloned automatically',
      'Ownership is moved; the original binding is no longer usable',
      'The compiler makes a shallow copy',
    ],
    correct: 'Ownership is moved; the original binding is no longer usable',
    studyRef: { cheats: 'https://cheats.rs/#ownership' } },
  { id: 'own-5', difficulty: 'Intermediate', type: 'boolean',
    title: 'You can have both a <code>&T</code> (shared reference) and a <code>&mut T</code> (mutable reference) to the same value active in the same scope.',
    correct: false,
    studyRef: { readme: '#borrowing' } },
  { id: 'own-6', difficulty: 'Intermediate', type: 'multi',
    title: 'Which of the following create a new owner of heap-allocated data? (select all that apply)',
    choices: ['String::from("x")', 'Box::new(5)', '&String::from("x")', 'vec![1,2,3]'],
    correct: ['String::from("x")', 'Box::new(5)', 'vec![1,2,3]'],
    studyRef: { cheats: 'https://cheats.rs/#ownership' } },
  { id: 'own-7', difficulty: 'Advanced', type: 'single',
    title: 'What does the Rust borrow checker prefer when inferring the lifetime of a borrow?',
    choices: [
      'The longest possible lifetime',
      'The shortest possible lifetime',
      'A lifetime equal to the enclosing function',
      'A \'static lifetime',
    ],
    correct: 'The shortest possible lifetime',
    studyRef: { readme: '#lifetimes' } },
  { id: 'own-8', difficulty: 'Advanced', type: 'code',
    title: 'What does this code do?',
    code: 'let mut v = vec![1, 2, 3];\nlet first = &v[0];\nv.push(4);\nprintln!("{}", first);',
    choices: [
      'Prints 1',
      'Fails to compile (shared borrow of v while v is mutably borrowed)',
      'Panics at runtime',
      'Prints 4',
    ],
    correct: 'Fails to compile (shared borrow of v while v is mutably borrowed)',
    studyRef: { cheats: 'https://cheats.rs/#ownership' } },
  { id: 'own-9', difficulty: 'Expert', type: 'single',
    title: 'Which statement about re-borrowing a <code>&mut T</code> as <code>&T</code> is correct?',
    choices: [
      'Re-borrowing ends the &mut T lifetime immediately',
      'Re-borrowing does not end the &mut T lifetime; it is suspended until the &T is dropped',
      'It is a compile error',
      'It creates a copy of the pointed-to value',
    ],
    correct: 'Re-borrowing does not end the &mut T lifetime; it is suspended until the &T is dropped',
    studyRef: { readme: '#lifetimes' } },
  { id: 'own-10', difficulty: 'Expert', type: 'boolean',
    title: 'Non-Lexical Lifetimes (NLL) allow the borrow checker to end a borrow before the end of its enclosing block when the borrow is no longer used.',
    correct: true,
    studyRef: { cheats: 'https://cheats.rs/#ownership' } },
]);
