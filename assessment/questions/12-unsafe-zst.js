window.registerTopic('Unsafe, Raw pointers, Pinning & ZST', [
  { id: 'unsafe-1', difficulty: 'Basic', type: 'boolean',
    title: 'Dereferencing a raw pointer in Rust requires an <code>unsafe</code> block.',
    correct: true,
    studyRef: { readme: '#raw-pointers', cheats: 'https://cheats.rs/#unsafe' } },
  { id: 'unsafe-2', difficulty: 'Basic', type: 'single',
    title: 'Which of the following is a Zero-Sized Type (ZST)?',
    choices: ['u8', '()', 'String', '*const u8'],
    correct: '()',
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-3', difficulty: 'Intermediate', type: 'multi',
    title: 'Which of the following are examples of Zero-Sized Types (ZSTs) in Rust? (select all that apply)',
    choices: ['PhantomData<T>', '()', 'Non-capturing closures', 'structs with no fields', 'u8', '[u8; 0]'],
    correct: ['PhantomData<T>', '()', 'Non-capturing closures', 'structs with no fields', '[u8; 0]'],
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-4', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>UnsafeCell&lt;T&gt;</code> is the only sound way in Rust to obtain a mutable reference through a shared reference.',
    correct: true,
    studyRef: { readme: '#raw-pointers' } },
  { id: 'unsafe-5', difficulty: 'Intermediate', type: 'single',
    title: 'Why does Rust emit <code>#[noalias]</code> LLVM metadata for <code>Box&lt;T&gt;</code>?',
    choices: [
      'To signal that the Box pointer cannot alias other Box pointers, enabling LLVM optimizations',
      'To prevent the Box from being dereferenced in unsafe code',
      'To mark the Box as Send + Sync',
      'To ensure the Box is never null',
    ],
    correct: 'To signal that the Box pointer cannot alias other Box pointers, enabling LLVM optimizations',
    studyRef: { readme: '#aliasing' } },
  { id: 'unsafe-6', difficulty: 'Intermediate', type: 'single',
    title: 'When you have two <code>Box</code> instances pointing to the same allocation (e.g., via raw pointer aliasing), aliasing is still not guaranteed because of <code>#[noalias]</code>. What is the recommended workaround?',
    choices: [
      'Use Arc instead of Box',
      'Wrap with MaybeUninit<T> which suppresses #[noalias]',
      'Use unsafe impl Sync for Box<T>',
      'Use std::hint::black_box',
    ],
    correct: 'Wrap with MaybeUninit<T> which suppresses #[noalias]',
    studyRef: { readme: '#aliasing' } },
  { id: 'unsafe-7', difficulty: 'Advanced', type: 'single',
    title: 'What is the purpose of <code>Pin&lt;P&gt;</code> in Rust?',
    choices: [
      'To prevent a value from being moved in memory after it has been pinned',
      'To ensure a value is heap-allocated',
      'To make a type Unpin',
      'To convert &T to &mut T safely',
    ],
    correct: 'To prevent a value from being moved in memory after it has been pinned',
    studyRef: { readme: '#pinning-and-self-referential-structs', cheats: 'https://cheats.rs/#pinning' } },
  { id: 'unsafe-8', difficulty: 'Advanced', type: 'code',
    title: 'What is the size of this type?',
    code: 'use std::marker::PhantomData;\nstruct Marker<T>(PhantomData<T>);\nprintln!("{}", std::mem::size_of::<Marker<String>>());',
    choices: ['8', '24', '0', '1'],
    correct: '0',
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-9', difficulty: 'Expert', type: 'boolean',
    title: '<code>NonNull&lt;T&gt;</code> is preferred over <code>*mut T</code> in safe abstractions because it is covariant in T and is Send/Sync when T: Send/T: Sync.',
    correct: true,
    studyRef: { readme: '#raw-pointers' } },
  { id: 'unsafe-10', difficulty: 'Expert', type: 'single',
    title: 'Self-referential structs are difficult to implement safely in Rust primarily because:',
    choices: [
      'They cannot implement Clone',
      'Moving the struct would invalidate internal pointers — Pin is used to prevent such moves',
      'They require unsafe impl Send',
      'The compiler prohibits fields that reference other fields',
    ],
    correct: 'Moving the struct would invalidate internal pointers — Pin is used to prevent such moves',
    studyRef: { readme: '#pinning-and-self-referential-structs' } },
]);
