window.registerTopic('Unsafe, Raw pointers, Pinning & ZST', [
  { id: 'unsafe-1', difficulty: 'Basic', type: 'boolean',
    title: 'Dereferencing a raw pointer in Rust requires an <code>unsafe</code> block.',
    correct: true,
    studyRef: { readme: '#raw-pointers', cheats: 'https://cheats.rs/#unsafe' } },
  { id: 'unsafe-2', difficulty: 'Basic', type: 'single',
    title: 'Which of the following is a Zero-Sized Type (ZST)?',
    choices: ['u8', '()', 'String', '*const u8'],
    correct: '()',
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-3', difficulty: 'Intermediate', type: 'multi',
    title: 'Which of the following are examples of Zero-Sized Types (ZSTs) in Rust? (select all that apply)',
    choices: ['PhantomData<T>', '()', 'Non-capturing closures', 'structs with no fields', 'u8', '[u8; 0]'],
    correct: ['PhantomData<T>', '()', 'Non-capturing closures', 'structs with no fields', '[u8; 0]'],
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-4', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>UnsafeCell&lt;T&gt;</code> is the only sound way in Rust to obtain a mutable reference through a shared reference.',
    correct: true,
    studyRef: { readme: '#raw-pointers' } },
  { id: 'unsafe-5', difficulty: 'Intermediate', type: 'single',
    title: 'Why does Rust emit <code>#[noalias]</code> LLVM metadata for <code>Box&lt;T&gt;</code>?',
    choices: [
      'To signal that the Box pointer cannot alias other Box pointers, enabling LLVM optimizations',
      'To prevent the Box from being dereferenced in unsafe code',
      'To mark the Box as Send + Sync',
      'To ensure the Box is never null',
    ],
    correct: 'To signal that the Box pointer cannot alias other Box pointers, enabling LLVM optimizations',
    studyRef: { readme: '#aliasing' } },
  { id: 'unsafe-6', difficulty: 'Intermediate', type: 'single',
    title: 'The compiler emits an LLVM <code>noalias</code> annotation for <code>Box&lt;T&gt;</code>, meaning it may assume the pointee is not aliased. What is the correct consequence of this guarantee?',
    choices: [
      'Creating two Box values that own/point to the same allocation is undefined behavior; use raw pointers (*const T / *mut T) for intentional aliasing',
      'Box<T> can be safely aliased as long as both boxes are immutable',
      'The noalias annotation only applies to Box<T> on the heap, not stack-allocated boxes',
      'Using Arc<T> instead of Box<T> removes the noalias annotation and makes aliasing safe',
    ],
    correct: 'Creating two Box values that own/point to the same allocation is undefined behavior; use raw pointers (*const T / *mut T) for intentional aliasing',
    studyRef: { readme: '#aliasing' } },
  { id: 'unsafe-7', difficulty: 'Advanced', type: 'single',
    title: 'What is the purpose of <code>Pin&lt;P&gt;</code> in Rust?',
    choices: [
      'To prevent a value from being moved in memory after it has been pinned',
      'To ensure a value is heap-allocated',
      'To make a type Unpin',
      'To convert &T to &mut T safely',
    ],
    correct: 'To prevent a value from being moved in memory after it has been pinned',
    studyRef: { readme: '#pinning-and-self-referential-structs', cheats: 'https://cheats.rs/#pinning' } },
  { id: 'unsafe-8', difficulty: 'Advanced', type: 'code',
    title: 'What is the size of this type?',
    code: 'use std::marker::PhantomData;\nstruct Marker<T>(PhantomData<T>);\nprintln!("{}", std::mem::size_of::<Marker<String>>());',
    choices: ['8', '24', '0', '1'],
    correct: '0',
    studyRef: { readme: '#zst' } },
  { id: 'unsafe-9', difficulty: 'Expert', type: 'boolean',
    title: '<code>NonNull&lt;T&gt;</code> is <code>!Send</code> and <code>!Sync</code> unconditionally (like raw pointers), but it is preferred over <code>*mut T</code> because it is covariant in T and enables the null-pointer niche optimization for <code>Option&lt;NonNull&lt;T&gt;&gt;</code>.',
    correct: true,
    studyRef: { readme: '#raw-pointers' } },
  { id: 'unsafe-10', difficulty: 'Expert', type: 'single',
    title: 'Self-referential structs are difficult to implement safely in Rust primarily because:',
    choices: [
      'They cannot implement Clone',
      'Moving the struct would invalidate internal pointers — Pin is used to prevent such moves',
      'They require unsafe impl Send',
      'The compiler prohibits fields that reference other fields',
    ],
    correct: 'Moving the struct would invalidate internal pointers — Pin is used to prevent such moves',
    studyRef: { readme: '#pinning-and-self-referential-structs' } },

  { id: 'unsafe-repr-1', difficulty: 'Intermediate', type: 'single',
    title: 'What does <code>#[repr(C)]</code> guarantee about a struct\'s memory layout?',
    choices: [
      'Fields are laid out in declaration order with C-compatible alignment and padding, making the struct safe to pass across FFI boundaries',
      'The struct contains no padding bytes between fields',
      'The struct is zero-sized',
      'The struct automatically implements <code>Copy</code>',
    ],
    correct: 'Fields are laid out in declaration order with C-compatible alignment and padding, making the struct safe to pass across FFI boundaries',
    studyRef: { cheats: 'https://cheats.rs/#memory-layout' } },

  { id: 'unsafe-repr-2', difficulty: 'Advanced', type: 'single',
    title: 'When is <code>#[repr(transparent)]</code> required?',
    choices: [
      'When a newtype wrapper must have exactly the same memory layout as its single non-ZST field — required for sound transmutation or FFI type aliasing',
      'When a struct contains only raw pointer fields',
      'When a struct needs to be passed by value across FFI without boxing',
      'When the struct derives both <code>Clone</code> and <code>Copy</code>',
    ],
    correct: 'When a newtype wrapper must have exactly the same memory layout as its single non-ZST field — required for sound transmutation or FFI type aliasing',
    studyRef: { cheats: 'https://cheats.rs/#memory-layout' } },

  { id: 'unsafe-repr-3', difficulty: 'Advanced', type: 'single',
    title: 'What does <code>#[repr(packed)]</code> do?',
    choices: [
      'Removes padding bytes between fields, packing them at one-byte alignment — may cause unaligned reads and requires <code>unsafe</code> to take references to fields',
      'Compresses the struct with run-length encoding at compile time',
      'Restricts field visibility to the current module',
      'Allocates the struct in static memory automatically',
    ],
    correct: 'Removes padding bytes between fields, packing them at one-byte alignment — may cause unaligned reads and requires <code>unsafe</code> to take references to fields',
    studyRef: { cheats: 'https://cheats.rs/#memory-layout' } },

  { id: 'unsafe-unpin', difficulty: 'Expert', type: 'single',
    title: 'What does the <code>Unpin</code> auto-trait signify for a type?',
    choices: [
      'The type can be safely moved even after being wrapped in <code>Pin</code> — <code>Pin</code> provides no additional guarantee for <code>Unpin</code> types',
      'The type must never be moved once placed on the heap',
      'The type uses a custom allocator that prevents movement',
      'The type does not implement <code>Drop</code>',
    ],
    correct: 'The type can be safely moved even after being wrapped in <code>Pin</code> — <code>Pin</code> provides no additional guarantee for <code>Unpin</code> types',
    studyRef: { cheats: 'https://cheats.rs/#pinning' } },
]);
