window.registerTopic('Traits & Generics', [
  { id: 'tg-1', difficulty: 'Basic', type: 'boolean',
    title: 'Trait objects (<code>dyn Trait</code>) enable dynamic dispatch at the cost of a vtable pointer.',
    correct: true,
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-2', difficulty: 'Basic', type: 'single',
    title: 'What does <code>impl Trait</code> in a return position mean?',
    choices: [
      'The function returns a trait object (dynamic dispatch)',
      'The function returns some concrete type that implements Trait, chosen by the compiler (static dispatch)',
      'The function returns a generic type parameter',
      'The function is async',
    ],
    correct: 'The function returns some concrete type that implements Trait, chosen by the compiler (static dispatch)',
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-3', difficulty: 'Intermediate', type: 'single',
    title: 'When should you prefer an associated type over a generic type parameter in a trait?',
    choices: [
      'When multiple implementations per type are needed',
      'When exactly one implementation per type is expected',
      'When the type is ?Sized',
      'When you need dynamic dispatch',
    ],
    correct: 'When exactly one implementation per type is expected',
    studyRef: { readme: '#generics' } },
  { id: 'tg-4', difficulty: 'Intermediate', type: 'single',
    title: '<code>From&lt;T&gt; for U</code> implies which other implementation automatically?',
    choices: ['Into&lt;T&gt; for U', 'Into&lt;U&gt; for T', 'From&lt;U&gt; for T', 'TryFrom&lt;T&gt; for U'],
    correct: 'Into&lt;U&gt; for T',
    studyRef: { readme: '#rust-conversions' } },
  { id: 'tg-5', difficulty: 'Intermediate', type: 'boolean',
    title: '<code>From&lt;T&gt; for T</code> (reflexive implementation) is provided by the standard library for all types.',
    correct: true,
    studyRef: { readme: '#rust-conversions' } },
  { id: 'tg-6', difficulty: 'Intermediate', type: 'multi',
    title: 'Which of the following are object-safe (can be used as <code>dyn Trait</code>)? (select all that apply)',
    choices: [
      'trait Foo { fn bar(&self); }',
      'trait Foo { fn bar() where Self: Sized; }',
      'trait Foo { fn clone(&self) -> Self; }',
      'trait Foo: Sized { }',
    ],
    correct: ['trait Foo { fn bar(&self); }', 'trait Foo { fn bar() where Self: Sized; }'],
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-7', difficulty: 'Advanced', type: 'code',
    title: 'What is the output?',
    code: 'trait Greet { fn hello(&self) -> &str { "hello" } }\nstruct A;\nstruct B;\nimpl Greet for A {}\nimpl Greet for B { fn hello(&self) -> &str { "world" } }\nfn print_greet(g: &dyn Greet) { println!("{}", g.hello()); }\nfn main() { print_greet(&A); print_greet(&B); }',
    choices: ['hello then hello', 'hello then world', 'world then hello', 'Compile error'],
    correct: 'hello then world',
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-8', difficulty: 'Advanced', type: 'single',
    title: 'What does monomorphization mean in the context of Rust generics?',
    choices: [
      'Generic code is erased at runtime like Java generics',
      'The compiler generates a specialized copy of the generic function for each concrete type it is called with',
      'Generic functions are only allowed to accept one type parameter',
      'Trait bounds are checked at runtime',
    ],
    correct: 'The compiler generates a specialized copy of the generic function for each concrete type it is called with',
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-9', difficulty: 'Expert', type: 'boolean',
    title: 'A Rust function that returns <code>impl Iterator</code> can return different concrete iterator types on different branches.',
    correct: false,
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
  { id: 'tg-10', difficulty: 'Expert', type: 'single',
    title: 'Which trait bound is required for a type parameter to be used as a trait object with <code>dyn Trait + Send</code>?',
    choices: [
      'The type must implement both Trait and Send',
      'Only Trait is required; Send is inferred',
      'The type must be \'static',
      'The type must implement Sync',
    ],
    correct: 'The type must implement both Trait and Send',
    studyRef: { cheats: 'https://cheats.rs/#traits' } },
]);
